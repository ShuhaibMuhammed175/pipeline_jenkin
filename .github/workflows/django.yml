name: Django CI

on:
  push:
    branches:
      - main

jobs:
  test:

    runs-on: self-hosted

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Migrations
        run: |
          python manage.py migrate

      - name: Run Django Tests
        run: |
          python manage.py test


# https://www.myntra.com/casual-shoes/roadster/the-roadster-lifestyle-co-men-laced-up-memory-foam-casual-shoes/30774848/buy